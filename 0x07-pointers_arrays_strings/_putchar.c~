#include <unistd.h>

/**
 *_putchar - writes char c to standard output
 * @c: the character to write
 *
 * Return: retruns 1 on success
 * -1 on errror, and errno is set appropriate
 */
int _putchar(char c)
{
  return (write(1, &c, 1));
}
